
import { Site } from './types.ts';

/**
 * MOCK_SITES is now empty to ensure the application starts with a clean database.
 * Users can add new sites via the "New Entry" button in the Site Repository.
 */
export const MOCK_SITES: Site[] = [];
